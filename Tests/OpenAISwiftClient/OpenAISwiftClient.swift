import XCTest
@testable import SwiftOpenAICleint

final class SwiftOpenAICleintTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftOpenAICleint().text, "Hello, World!")
    }
}
